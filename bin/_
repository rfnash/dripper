#!/usr/bin/env bash

find_env_file() {
  local root="$PWD"

  while [ -n "$root" ]; do
    if [ -e "${root}/.env" ]; then
      source ${root}/.env
      echo "=> Sourced ${root}/.env"
      break
    fi
    root="${root%/*}"
  done
}

find_env_file
exec "$@"

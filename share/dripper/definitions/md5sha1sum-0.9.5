#!/usr/bin/env bash

PACKAGE=md5sha1sum
VERSION=0.9.5

set_variables
URL=http://www.microbrew.org/tools/md5sha1sum/md5sha1sum-0.9.5.tar.gz

if _install; then
  cd $SRC_PATH
  curl --silent -O $URL
  tar xzf $STOW.tar.gz
  cd $STOW
  ./configure \
    --prefix=$PREFIX
  make install
fi
